version: '3.8'

services:
  app:
    name: app
    build: .
    command: celery -A app.celery worker --loglevel=info
    depends_on:
      - redis
    volumes:
      - .:/app
    ports:
      - "5000:5000"
  
  redis:
    name: redis
    image: redis:latest
    ports:
      - "6379:6379"
